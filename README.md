# Kohzu Serialport Terminal Program
This is a simple program to enter text commands to be sent via serial port to a Kohzu SC-410 or SC-210 controller.
It assumes that you are using COM4.  Change this in code if you use a different port.
Type 'exit' or 'EXIT' to quit
